# FireSight-ST: DINOv2-Small + Full SAFE + Temporal Motion Fusion
# For cameras with sequential frames (60s or 360s intervals)

model:
  type: firesight
  num_classes: 2
  backbone_type: dinov2
  backbone_channels: 256
  freeze_backbone: false
  use_dcm: true
  use_fam: true
  use_tm: true
  use_temporal: true     # Enable temporal motion-guided fusion
  head_type: yolox
  input_size: [1022, 1022]

data:
  train_img_dir: /home/whamidouche/ssdprivate/datasets/data_v3_and_imerit_upto_april06_2classes_40_unseen_cams_cleaned/train/images
  train_label_dir: /home/whamidouche/ssdprivate/datasets/data_v3_and_imerit_upto_april06_2classes_40_unseen_cams_cleaned/train/labels
  val_img_dir: /home/whamidouche/ssdprivate/datasets/data_v3_and_imerit_upto_april06_2classes_40_unseen_cams_cleaned/test_random/images
  val_label_dir: /home/whamidouche/ssdprivate/datasets/data_v3_and_imerit_upto_april06_2classes_40_unseen_cams_cleaned/test_random/labels
  batch_size: 32
  num_workers: 16
  mosaic_prob: 0.8  # Lower mosaic for temporal consistency
  mixup_prob: 0.3
  multi_frame: true
  frame_interval: 60

optimizer:
  lr: 1.28e-3
  momentum: 0.9
  weight_decay: 0.0005

training:
  max_epochs: 50
  warmup_epochs: 5
  val_interval: 1
  save_interval: 2
  grad_clip: 35.0

output_dir: runs/firesight_st_1024
log_interval: 50
